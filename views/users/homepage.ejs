<% include("../layouts/header.ejs") %>
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.5/dist/css/bootstrap.min.css" rel="stylesheet">
<link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">
<link href="https://fonts.googleapis.com/css2?family=Poppins:wght@700&display=swap" rel="stylesheet">
<link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" rel="stylesheet">
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/toastify-js/src/toastify.min.css">

<style>
  body {
    font-family: 'Poppins', sans-serif;
  }
  .navbar-brand {
    font-weight: 700;
    font-size: 1.6rem;
    color: #1a1517;
    letter-spacing: 0.5px;
  }

  .brand-dot {
    width: 10px;
    height: 10px;
    background: #C586A5;
    border-radius: 50%;
    display: inline-block;
  }

  .navbar-nav {
    gap: 1.5rem;
  }

  .navbar-nav .nav-link {
    font-weight: 500;
    color: #333;
    transition: color 0.3s;
  }

  .navbar-nav .nav-link:hover,
  .navbar-nav .nav-link.active {
    color: #C586A5;
  }

  .badge-dot {
    position: absolute;
    top: -5px;
    right: -5px;
    width: 10px;
    height: 10px;
    background-color: red;
    border-radius: 50%;
    border: 1px solid white;
  }
  .category-card {
    transition: all 0.3s ease;
  }
  .category-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 8px 16px rgba(0,0,0,0.1);
  }
/* Card Styling */
.card {
  border: none;
  border-radius: 12px;
  overflow: hidden;
  transition: transform 0.3s ease, box-shadow 0.3s ease;
}

.card:hover {
  transform: translateY(-5px);
  box-shadow: 0 12px 24px rgba(0, 0, 0, 0.15);
}

.card-img-top {
  width: 100%;
  height: 280px;
  object-fit: contain;
  object-position: center;
  transition: transform 0.4s ease;
  display: block;
}


.card:hover .card-img-top {
  transform: scale(1.05);
}

/* Quick View Overlay */
.card-overlay {
  position: absolute;
  top: 0;
  left: 0;
  height: 100%;
  width: 100%;
  background: rgba(0, 0, 0, 0.45);
  opacity: 0;
  transition: all 0.3s ease;
}

.card:hover .card-overlay {
  opacity: 1;
}

.card-overlay a {
  opacity: 0;
  transition: opacity 0.3s ease;
}

.card:hover .card-overlay a {
  opacity: 1;
}

/* Wishlist Button */
.wishlist-btn {
  position: absolute;
  top: 12px;
  right: 12px;
  background: transparent;
  border: none;
  cursor: pointer;
  z-index: 2;
  padding: 0;
}

.wishlist-btn i {
  color: #ebeaea;
  font-size: 22px;
  transition: transform 0.3s ease, color 0.3s ease;
}

.wishlist-btn:hover i {
  color: #e60039;
  transform: scale(1.2);
}
.category-card {
  transition: transform 0.3s ease, box-shadow 0.3s ease;
}

.category-card:hover {
  transform: translateY(-5px);
  box-shadow: 0 10px 20px rgba(0,0,0,0.15);
}


.category-img {
  object-fit: contain;
  object-position: center;
  width: 100%;
  height: 280px; 
  border-top-left-radius: 0.375rem; 
  border-top-right-radius: 0.375rem;
  transition: transform 0.4s ease;
  display: block;
}


.card-title {
  font-size: 1.2rem;
  color: #212529;
}
.banner-section {
  background: url('/images/hero_img.png') no-repeat center center;
  background-size: cover;
  position: relative;
  min-height: 70vh; /* Creates a large, impactful banner */
  z-index: 1;
  overflow: hidden;
}

.banner-section::before {
  content: '';
  position: absolute;
  top: 0; left: 0; right: 0; bottom: 0;
  background: rgba(0, 0, 0, 0.5); /* Semi-transparent overlay */
  z-index: -1;
}

.banner-section h1,
.banner-section h5,
.banner-section a {
  z-index: 2;
}

@media (max-width: 768px) {
  .banner-section h1 {
    font-size: 2.5rem;
  }
  .banner-section h5 {
    font-size: 1rem;
  }
}


</style>

<nav class="navbar navbar-expand-lg bg-white border-bottom sticky-top py-3">
  <div class="container">
    <a href="/" class="navbar-brand d-flex align-items-center gap-2">
      18-CREW <span class="brand-dot"></span>
    </a>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarContent">
      <ul class="navbar-nav mx-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link <%= typeof page !== 'undefined' && page === 'home' ? 'active' : '' %>" href="/">HOME</a>
        </li>
        <li class="nav-item">
          <a class="nav-link <%= typeof page !== 'undefined' && page === 'shop' ? 'active' : '' %>" href="/shop">COLLECTION</a>
        </li>
        <li class="nav-item">
          <a class="nav-link <%= typeof page !== 'undefined' && page === 'about' ? 'active' : '' %>" href="#">ABOUT</a>
        </li>
        <li class="nav-item">
          <a class="nav-link <%= typeof page !== 'undefined' && page === 'contact' ? 'active' : '' %>" href="#">CONTACT</a>
        </li>

        <% if (locals?.user || user) { %>
          <li class="nav-item dropdown d-lg-none">
            <a class="nav-link dropdown-toggle text-uppercase fw-bold" href="#" role="button" data-bs-toggle="dropdown">
              <%= user.name %>
            </a>
            <ul class="dropdown-menu">
              <li><a class="dropdown-item text-danger" href="/logout">Logout</a></li>
            </ul>
          </li>
        <% } else { %>
          <li class="nav-item d-lg-none">
            <a class="nav-link" href="/signin"><i class="bi bi-person me-2"></i>Sign In</a>
          </li>
        <% } %>
      </ul>

      <div class="d-none d-lg-flex align-items-center gap-3">
        <% if (locals?.user || user) { %>
          <div class="dropdown">
            <a class="nav-link dropdown-toggle text-uppercase fw-bold" href="#" role="button" data-bs-toggle="dropdown">
              <%= user.name %>
            </a>
            <ul class="dropdown-menu dropdown-menu-end">
                <li><a class="dropdown-item" href="/userProfile">Profile</a></li>
              <li><hr class="dropdown-divider"></li>
              <li><a class="dropdown-item text-danger" href="/logout">Logout</a></li>
            </ul>
          </div>
        <% } else { %>
          <a href="/signin" class="text-dark" style="font-size: 1.5rem;">
            <i class="bi bi-person"></i>
          </a>
        <% } %>
        <a href="/wishlist" class="text-dark position-relative" style="font-size: 1.1rem;" aria-label="Wishlist<%= wishlistCount > 0 ? ` with ${wishlistCount} items` : '' %>">
          <i class="bi bi-heart"></i>
          <% if (wishlistCount > 0) { %>
            <span class="badge-dot"></span>
          <% } %>
        </a>
        <a href="/getCartPage" class="nav-link position-relative" aria-label="Cart<%= cartCount > 0 ? ` with ${cartCount} items` : '' %>">
          <i class="bi bi-bag"></i>
          <% if (cartCount > 0) { %>
            <span class="badge-dot"></span>
          <% } %>
        </a>
      </div>
    </div>
  </div>
</nav>


<!-- HERO SECTION -->
<section class="banner-section d-flex align-items-center text-white text-center">
  <div class="container py-5">
    <h5 class="text-uppercase text-light mb-2">Our Bestseller</h5>
    <h1 class="display-3 fw-bold mb-4">Latest Arrivals</h1>
    <a href="/shop" class="btn btn-outline-light btn-lg px-4">Shop Now</a>
  </div>
</section>


<!-- CATEGORY SECTION -->
<section class="py-5 bg-light">
  <div class="container">
    <h2 class="text-center mb-5 fw-bold text-dark">Shop by Category</h2>
    <div class="row g-4 justify-content-center">
      <% for (let i = 0; i < category.length; i++) { %>
        <div class="col-6 col-sm-6 col-md-4 col-lg-3">
          <div class="card h-100 border-0 shadow-sm rounded-4 overflow-hidden category-card">
            <div class="ratio ratio-1x1 bg-light">
              <img 
                src="/uploads/re-image/<%=category[i].image%>" 
                class="img-fluid object-fit-cover" 
                alt="<%= category[i].name %>" 
                style="width: 100%; height: 100%; object-fit: cover;">
            </div>
            <div class="card-body d-flex flex-column justify-content-center align-items-center">
              <a href="/filter?category=<%= category[i]._id %>" class="stretched-link text-decoration-none text-dark fw-semibold fs-5 text-center">
                <%= category[i].name %>
              </a>
            </div>
          </div>
        </div>
      <% } %>
    </div>
  </div>
</section>





<!-- LATEST COLLECTIONS -->
<section class="py-5">
  <div class="container">
    <h2 class="text-center mb-3">Latest Collections</h2>
    <p class="text-center text-muted mb-5">Discover new trends and styles curated just for you.</p>
    <div class="row g-4">
      <% for(let i=0; i<4; i++) { %>
        <div class="col-6 col-md-4 col-lg-3">
          <div class="card h-100 text-center position-relative overflow-hidden">
            
            <!-- Wishlist Button -->
            <button class="wishlist-btn"  onclick="addToWishlist('<%= products[i]._id %>')"><a href="#">
              <i class="fa-regular fa-heart"></i></a>
            </button>
            
            
              <img src="/uploads/re-image/<%= products[i].productImage[0] %>" class="card-img-top" alt="<%= products[i].productName %>">
          
        
            <!-- Quick View Overlay -->
            <div class="card-overlay d-flex justify-content-center align-items-center">
              <a href="/productDetails?id=<%= products[i]._id %>" class="btn btn-dark btn-sm">Quick View</a>
            </div>
        
            <div class="card-body">
              <h5 class="card-title fs-6"><%= products[i].productName %></h5>
              <p class="text-muted">₹ <%= products[i].salePrice %></p>
            </div>
        
          </div>
        </div>
        
      <% } %>
    </div>
  </div>
</section>


<!-- INFO ICONS -->
<section class="py-5 bg-light">
  <div class="container">
    <div class="row text-center g-4">
      <div class="col-12 col-md-4">
        <i class="bi bi-truck fs-2"></i>
        <p class="mt-2">Free Nationwide Shipping</p>
      </div>
      <div class="col-12 col-md-4">
        <i class="bi bi-arrow-counterclockwise fs-2"></i>
        <p class="mt-2">7 Days Return Policy</p>
      </div>
      <div class="col-12 col-md-4">
        <i class="bi bi-headset fs-2"></i>
        <p class="mt-2">Best Customer Support</p>
      </div>
    </div>
  </div>
</section>

<!-- SUBSCRIBE -->
<section class="py-5">
  <div class="container text-center">
    <h3>Subscribe Now & Get 20% Off</h3>
    <form action="/subscribe" method="POST" class="row justify-content-center mt-4 g-2">
      <div class="col-12 col-md-4">
        <input type="email" class="form-control" name="email" placeholder="Enter your email" required>
      </div>
      <div class="col-12 col-md-2">
        <button type="submit" class="btn btn-dark w-100">Subscribe</button>
      </div>
    </form>
  </div>
</section>

<!-- FOOTER -->
<footer class="py-5 border-top">
  <div class="container">
    <div class="row gy-4 text-center text-md-start align-items-start">
      
      <div class="col-md-6 col-lg-5">
        <h4 class="fw-bold">18-CREW<span class="text-danger">.</span></h4>
        <p class="text-muted small mb-0">
          Lorem Ipsum is simply dummy text of the printing and typesetting industry. Lorem Ipsum has been the industry's standard dummy text ever since the 1500s.
        </p>
      </div>
      
      <div class="col-md-3 col-lg-3">
        <h6 class="fw-semibold mb-3">COMPANY</h6>
        <ul class="list-unstyled text-muted small">
          <li><a href="/home" class="text-decoration-none text-muted">Home</a></li>
          <li><a href="/about" class="text-decoration-none text-muted">About us</a></li>
          <li><a href="/delivery" class="text-decoration-none text-muted">Delivery</a></li>
          <li><a href="/privacy" class="text-decoration-none text-muted">Privacy policy</a></li>
        </ul>
      </div>

      <div class="col-md-3 col-lg-4">
        <h6 class="fw-semibold mb-3">GET IN TOUCH</h6>
        <p class="mb-1 text-muted small">+1-212-456-7890</p>
        <p class="mb-0 text-muted small">18CREW@gmail.com</p>
      </div>

    </div>

    <hr class="my-5" />
    <div class="text-center text-muted small">
      Copyright © 2024 @ 18-CREW.dev – All Right Reserved.
    </div>
  </div>
</footer>
<script src="https://cdn.jsdelivr.net/npm/toastify-js"></script>
<script>
  async function addToWishlist(productId) {
    try {
      const response = await fetch("/addToWishlist", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ productId })
      });
      const data = await response.json();
      
      if (response.ok && data.status) {
        showToast({
          message: "Product added to wishlist!",
          type: "success",
          icon: "✓"
        });
      } else {
        showToast({
          message: data.message || 'Failed to add to wishlist',
          type: "error",
          icon: "✕"
        });
      }
    } catch {
      showToast({
        message: 'Network error - please try again',
        type: "error",
        icon: "⚠"
      });
    }
  }

  function showToast({ message, type, icon }) {
    const colors = {
      success: {
        bg: "linear-gradient(to right, #00b09b, #96c93d)",
        text: "#fff"
      },
      error: {
        bg: "linear-gradient(to right, #ff5f6d, #ffc371)",
        text: "#fff"
      }
    };

    Toastify({
      text: `${icon} ${message}`,
      duration: type === "success" ? 2000 : 3000,
      close: true,
      gravity: "top",
      position: "right",
      style: {
        background: colors[type].bg,
        color: colors[type].text,
        borderRadius: "8px",
        boxShadow: "0 4px 12px rgba(0,0,0,0.15)",
        padding: "12px 20px",
        fontSize: "14px",
        fontWeight: "500",
        display: "flex",
        alignItems: "center",
        gap: "8px"
      },
      stopOnFocus: true,
      onClick: function() {}
    }).showToast();
  }
</script>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.5/dist/js/bootstrap.bundle.min.js"></script>
<% include("../layouts/footer.ejs") %>




